<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/search_rvItem_imageProfile"
        android:src="@mipmap/ic_launcher"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/search_rvItem_imageProfile"
        android:layout_marginStart="12dp"
        android:orientation="vertical"
        android:layout_centerVertical="true">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/search_rvItem_username"
            android:maxLines="1"
            android:textSize="20sp"
            android:fontFamily="@font/lato_regular"
            android:text="Username"
            android:textColor="@color/rv_text"/>

        <TextView
            android:layout_marginTop="3dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/search_rvItem_fullname"
            android:maxLines="1"
            android:textSize="15sp"
            android:fontFamily="@font/lato_regular"
            android:text="Fullname"
            android:textColor="@color/rv_secondary_text"/>
    </LinearLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:background="@drawable/followbtn"
        android:id="@+id/search_rvItem_followBtn"
        android:textColor="@color/text"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:textSize="14dp"
        android:visibility="gone"
        android:padding="5dp"/>
</RelativeLayout>